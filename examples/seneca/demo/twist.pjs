// Twist
// Based on a demo by Casey Reas and Ben Fry

int t, i;

size(800,600);
smooth();
strokeWeight(20);
background(0,0,0,0);

for(int i = 0; i < width; i++) {
  float r = random(255);
  float x = random(0, width);
  stroke(100,r,r, 70);
  line(i, 0, x, height);
}